import type { Submission } from "@prisma/client";
import { SectionHeading } from "@/components/section-heading";
import { SurfaceCard } from "@/components/surface-card";
import { StatusBadge } from "@/components/status-badge";
import { formatDate, safeJsonParse } from "@/lib/utils";

export function SubmissionCard({ submission }: { submission: Submission | null | undefined }) {
  if (!submission) {
    return (
      <SurfaceCard>
        <SectionHeading title="Submission" description="Draft or live submission artifacts will appear here once the mission reaches the final stage." />
        <div className="text-sm text-[var(--muted-foreground)]">No submission artifact has been created yet.</div>
      </SurfaceCard>
    );
  }

  const changedFiles = safeJsonParse<string[]>(submission.changedFiles, []);

  return (
    <SurfaceCard>
      <div className="flex items-start justify-between gap-4">
        <SectionHeading title="Submission" description="Branch, commit, and PR content generated by the Submitter Agent." />
        <StatusBadge value={submission.submissionStatus} />
      </div>

      <div className="grid gap-4 lg:grid-cols-2">
        <div className="space-y-4">
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">Branch Name</div>
            <div className="mt-2 text-sm font-medium text-[var(--foreground)]">{submission.branchName}</div>
          </div>
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">Commit Message</div>
            <div className="mt-2 text-sm font-medium text-[var(--foreground)]">{submission.commitMessage ?? "No commit created in dry-run mode."}</div>
          </div>
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">PR Title</div>
            <div className="mt-2 text-sm font-medium text-[var(--foreground)]">{submission.prTitle}</div>
          </div>
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">Last Updated</div>
            <div className="mt-2 text-sm font-medium text-[var(--foreground)]">{formatDate(submission.updatedAt)}</div>
          </div>
        </div>

        <div className="space-y-4">
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">Verification Summary</div>
            <div className="mt-2 whitespace-pre-line text-sm leading-6 text-[var(--foreground)]">{submission.verificationSummary}</div>
          </div>
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">Changed Files</div>
            <div className="mt-2 text-sm leading-6 text-[var(--foreground)]">
              {changedFiles.length > 0 ? changedFiles.join(", ") : "No repository changes were persisted in the current mission mode."}
            </div>
          </div>
          <div className="rounded-2xl border border-[var(--border)] bg-[var(--panel-muted)] p-4">
            <div className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--muted-foreground)]">PR Body</div>
            <pre className="mt-2 overflow-x-auto whitespace-pre-wrap text-sm leading-6 text-[var(--foreground)]">{submission.prBody}</pre>
          </div>
        </div>
      </div>
    </SurfaceCard>
  );
}
